import java.util.*;

public class RotateArray {
    
    public static int[] rightRotateArray(int[] arr, int n) {
        int length = arr.length;
        int mid = length / 2;
        int secondHalfStart = mid;
        int secondHalfLength = length - secondHalfStart;
        
        if (secondHalfLength == 0) {
            return arr;
        }
        
        int effectiveN = n % secondHalfLength;
        
        if (effectiveN == 0) {
            return arr;
        }
        
        int[] result = new int[length];
        
        for (int i = 0; i < secondHalfStart; i++) {
            result[i] = arr[i];
        }
        
        for (int i = 0; i < secondHalfLength; i++) {
            int sourceIndex = secondHalfStart + ((i + effectiveN) % secondHalfLength);
            result[secondHalfStart + i] = arr[sourceIndex];
        }
        
        return result;
    }
    
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        int nElements = scanner.nextInt();
        
        int[] arr = new int[nElements];
        for (int i = 0; i < nElements; i++) {
            arr[i] = scanner.nextInt();
        }
        
        int nRotations = scanner.nextInt();
        
        int[] result = rightRotateArray(arr, nRotations);
        
        for (int i = 0; i < result.length; i++) {
            System.out.print(result[i]);
            if (i < result.length - 1) {
                System.out.print(" ");
            }
        }
        System.out.println();
        
        scanner.close();
    }
}
